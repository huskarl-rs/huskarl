[package]
name = "huskarl-conformance"
version = "0.1.0"
edition = "2024"
publish = false

[features]
# Enables integration tests that require a running OpenID conformance suite instance.
# See README for setup instructions.
conformance-suite-tests = []

[dependencies]
bytes = "1"
http = "1"
huskarl = { version = "0.7.0-pre.1", features = ["url", "authorization-flow-loopback"] }
huskarl-reqwest = { version = "0.6", features = ["rustls-tls"] }
reqwest = { version = "0.13", default-features = false, features = ["cookies", "json", "query"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", default-features = false, features = ["rt", "sync", "macros", "time"] }
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
huskarl-crypto-native = { version = "0.7" }
huskarl-testkit = { path = "../huskarl-testkit" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
